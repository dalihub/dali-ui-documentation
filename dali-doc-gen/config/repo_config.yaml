repos:
  dali-core:
    internal_url: "git@github.sec.samsung.net:tizen/dali-core.git"
    external_url: "https://github.com/dalihub/dali-core.git"
    path: "repos/dali-core"
    internal_branch: "tizen"
    external_branch: "master"
    api_dirs:
      - "dali/public-api"
      - "dali/devel-api"
      - "dali/integration-api"
  dali-adaptor:
    internal_url: "git@github.sec.samsung.net:tizen/dali-adaptor.git"
    external_url: "https://github.com/dalihub/dali-adaptor.git"
    path: "repos/dali-adaptor"
    internal_branch: "tizen"
    external_branch: "master"
    api_dirs:
      - "dali/public-api"
      - "dali/devel-api"
      - "dali/integration-api"
  dali-ui:
    internal_url: "git@github.sec.samsung.net:NUI/dali-ui.git"
    external_url: "https://github.com/dalihub/dali-ui.git"
    path: "repos/dali-ui"
    internal_branch: "devel"
    external_branch: "devel"
    api_dirs:
      - "dali-ui-foundation/public-api"
      - "dali-ui-foundation/devel-api"
      - "dali-ui-foundation/integration-api"
      - "dali-ui-components/public-api"
      - "dali-ui-components/integration-api"

# ============================================================
# Manual Feature Injection (Phase 1.5)
# 여기에 정의된 Feature는 물리적 디렉토리 분류와 무관하게
# feature_clusterer.py가 강제로 feature_map에 삽입합니다.
# ============================================================
manual_features:
  - feature: "view"
    display_name: "View (Base UI Object)"
    source_package: "dali-ui"
    base_class: "Dali::Ui::View"
    description: "The primary UI object for DALi application developers. View inherits from Actor but provides a higher-level API designed for app development."
    force_tree_review: true   # taxonomy_reviewer.py가 반드시 Tree 구조 여부를 검토

  - feature: "actors"
    display_name: "Actor (Scene Graph Node)"
    source_package: "dali-core"
    base_class: "Dali::Actor"
    description: "Low-level scene graph node. In dali-ui apps, developers use View instead of Actor directly. Actor knowledge is background context."
    audience: "platform"
    suppress_doc: true        # actors.md 생성 안 함 — View 문서에 통합
    merge_into: "view"        # View 문서 생성 시 Actor public-api를 inherited context로 포함

  - feature: "custom-actor"
    display_name: "CustomActor"
    source_package: "dali-core"
    description: "Internal extension mechanism. App and platform developers do not use CustomActor directly."
    suppress_doc: true        # custom-actor.md 생성 안 함, 어디에도 통합하지 않음
